# 雪笠微光 DESIGN.md

本设计规范用于统一 `雪笠微光` 的 Stitch 设计系统与后续前端实现。

唯一总原则：

- `UI 外壳、控件、排版、装饰语言` 必须保持严格黑白系统
- `视频封面、作品缩略图、创作内容素材` 可以保留原始色彩
- 内容素材不得反向污染 UI 的颜色 token、按钮状态、边框颜色或文本颜色

---

## 1. 整体视觉语言

### 1.1 风格关键词

- 深黑
- 克制
- 冷静
- 留白
- 档案馆感
- 编辑感
- 展示优先

### 1.2 视觉目标

界面应当像一个面向影像与创作作品的黑白档案馆，而不是普通视频平台。整体氛围强调沉静、专业、可浏览、可收藏，避免娱乐化、彩色化、夸张化装饰。

### 1.3 视觉基调

- 默认使用深黑背景，不使用彩色背景
- 通过灰阶层次而非彩色层次区分区域关系
- 通过留白、字号、粗细、边框和卡片体块建立信息层级
- 内容封面允许有色彩，但 UI 始终保持中立，不跟随封面色偏
- 动效克制，优先体现平滑、缓慢、稳定，而不是跳跃感

### 1.4 禁止项

- 禁止彩色按钮
- 禁止彩色发光
- 禁止高饱和渐变作为 UI 主体
- 禁止过重玻璃拟态
- 禁止用阴影代替层级结构
- 禁止用随机圆角和随机边框破坏系统统一性

---

## 2. 颜色系统

### 2.1 原则

颜色系统仅由 `黑 / 白 / 灰` 组成。所有 UI 元素必须从语义 token 取值，不允许组件自行定义任意灰度。

### 2.2 基础灰阶

```txt
--bg-0: #000000;      // 全站主背景
--bg-1: #0A0A0B;      // 一级区块背景
--bg-2: #111214;      // 二级区块 / 卡片背景
--bg-3: #17181B;      // 悬浮层 / 激活卡片背景

--line-1: #232428;    // 默认边框
--line-2: #2F3136;    // 强边框 / 选中边框
--line-3: #FFFFFF1F;  // 浅色描边 / 叠加边框

--text-1: #FFFFFF;    // 主文本
--text-2: #C9CBD1;    // 次文本
--text-3: #8B8E97;    // 弱文本
--text-4: #5F636B;    // 禁用 / 远景文本

--white-soft: #F5F5F3; // 亮底按钮或反转块
--black-soft: #050505; // 黑底中的最深压暗层
```

### 2.3 语义颜色规则

- 页面主背景使用 `--bg-0`
- 内容区块、导航、抽屉、筛选栏使用 `--bg-1` 或 `--bg-2`
- 默认边框使用 `--line-1`
- hover 或 focus 可提升到 `--line-2`
- 主标题使用 `--text-1`
- 描述、说明、统计数据使用 `--text-2`
- 标签、辅助信息、时间、来源、空状态说明使用 `--text-3`
- 禁用态使用 `--text-4`

### 2.4 反转规则

仅以下场景允许黑白反转：

- 主 CTA 按钮
- 当前选中筛选项
- 顶级登录/提交等关键操作

反转后规则：

- 背景使用 `--white-soft`
- 文本使用 `#0A0A0B`
- 不额外叠加彩色描边

### 2.5 内容封面规则

- 封面图可以保留彩色
- 封面图上叠加文字时，必须增加 `40% - 72%` 黑色遮罩以保证可读性
- 封面图不得决定按钮颜色、图标颜色、标签颜色
- 封面图外层卡片、信息区、边框、标签始终使用黑白 token

---

## 3. 字体层级

### 3.1 字体策略

- 中文主字体：`Noto Sans SC`, `PingFang SC`, `Hiragino Sans GB`, sans-serif
- 英文主字体：`Geist`, `Inter`, sans-serif
- 数字与元信息字体：`Geist Mono`, `JetBrains Mono`, monospace

说明：

- 中文内容优先保持中性、现代、可读
- 英文标题可更干净、更有展示感
- 时间、编号、来源、计数等元信息适合使用等宽字体增强档案感

### 3.2 字重规则

- `700`：品牌名、主标题、卡片标题
- `600`：区块标题、按钮文字、筛选激活项
- `500`：正文、标签、导航
- `400`：辅助说明、统计文案、次级信息

### 3.3 字号层级

```txt
Display XL: 64/72   用于首页 Hero 主标题
Display L : 48/56   用于首页重要标题
H1        : 36/44   用于详情页、个人页主标题
H2        : 28/36   用于区块标题
H3        : 22/30   用于卡片重点标题 / 列表页标题
Body L    : 18/28   用于导语、说明性文本
Body M    : 16/24   用于常规正文
Body S    : 14/22   用于标签、元信息
Caption   : 12/18   用于时间、来源、状态、计数
Mono Meta : 12/16   用于时长、来源、编号、统计值
```

### 3.4 排版规则

- 标题尽量短，不做过多断行
- 中文标题优先左对齐
- 行宽控制在舒适阅读范围，正文单列宽度建议不超过 `720px`
- 元信息优先单行排列，用分隔点或细线断开
- 字号层级不超过 `6` 个核心档位，避免系统失控

---

## 4. 间距、圆角、边框、阴影规则

### 4.1 间距系统

采用 `4px` 基准栅格，核心值如下：

```txt
4, 8, 12, 16, 20, 24, 32, 40, 48, 64, 80, 96
```

推荐用途：

- 组件内微间距：`8 / 12 / 16`
- 卡片内边距：`16 / 20 / 24`
- 区块间距：`32 / 40 / 48`
- 页面级留白：`64 / 80 / 96`

### 4.2 圆角系统

```txt
R1: 10px   小标签、输入框、小按钮
R2: 14px   标准按钮、筛选项、搜索框
R3: 18px   标准卡片、面板、抽屉
R4: 24px   Hero 卡片、大型容器
R5: 32px   极少数超大展示模块
```

规则：

- 同一页面主流卡片尽量统一在 `R3`
- 大按钮与搜索框优先使用 `R2`
- Hero、播放器容器、Banner 可使用 `R4`
- 避免在一个视口内混用过多圆角级别

### 4.3 边框规则

- 默认边框：`1px solid var(--line-1)`
- 强调边框：`1px solid var(--line-2)`
- 浅色描边：用于亮底按钮或封面叠层，使用 `1px solid rgba(255,255,255,0.12~0.18)`
- 不使用高对比纯白实线大面积包裹组件

### 4.4 阴影规则

阴影只用于微弱抬升，不用于制造彩色氛围。

```txt
Shadow S: 0 6px 18px rgba(0,0,0,0.22)
Shadow M: 0 10px 30px rgba(0,0,0,0.28)
Shadow L: 0 16px 48px rgba(0,0,0,0.36)
```

规则：

- 普通卡片默认可无阴影，仅靠灰阶分层
- hover 时可增加 `Shadow S`
- 弹层、抽屉、悬浮面板使用 `Shadow M`
- Hero 或大型浮层必要时使用 `Shadow L`
- 禁止蓝光、紫光、彩色外发光

### 4.5 动效规则

- 过渡时长优先 `180ms / 240ms / 320ms`
- easing 优先平滑缓出
- hover 仅做轻微位移、描边增强、背景提亮
- 页面切换应慢于按钮 hover，强调沉稳而非活泼

---

## 5. 页面布局规则

### 5.1 总体布局

- 桌面端采用 `12` 列栅格
- 平板采用 `8` 列栅格
- 移动端采用 `4` 列栅格
- 主内容区域优先左对齐，不强求绝对居中
- 页面边缘保持充足呼吸感，避免内容贴边

### 5.2 容器宽度

```txt
Full Bleed Hero: 100%
Content Max Width: 1440px
Reading Column: 720px
Wide Media Area: 1200px
```

### 5.3 顶部导航规则

- 导航高度建议 `72px - 80px`
- Logo 位于左侧，功能操作位于右侧
- 导航项数量控制在 `3 - 6` 个
- 当前页用下划线、亮底或更高亮文本标识，不使用彩色高亮

### 5.4 首页规则

- 首页首屏允许全宽视觉封面，但必须以深色遮罩压住背景噪音
- Hero 文案不宜过长，主标题、导语、双 CTA 即可
- 首页下方内容区以精选卡片流或分组卡片流呈现
- 首屏与内容流之间要有明确分隔，优先使用深浅背景切换或留白断层

### 5.5 Archive / 聚合页规则

- 筛选系统优先水平展开，必要时可配合侧栏
- 卡片支持等宽不等高，但信息区结构必须统一
- 筛选条、搜索条、排序条必须固定成一套一致骨架
- 大量内容页优先依靠间距和灰阶组织，不增加多余分隔线

### 5.6 视频详情页规则

- 播放器或封面容器必须成为第一视觉焦点
- 标题、来源、发布时间、收藏操作紧跟媒体区出现
- 描述文案保持高可读性，避免过长单行
- 标签、来源、状态、分享等操作用次级视觉承接，不抢主标题

### 5.7 个人中心 / 收藏页规则

- 支持左侧导航 + 右侧内容，或顶部概览 + 下方卡片流
- 收藏夹、标签、检索条件必须形成可重复使用的侧边结构
- 内容主区始终优先展示卡片成果，不让筛选 UI 压过内容

### 5.8 响应式规则

- 移动端先折叠非核心操作
- Hero 文案和卡片信息优先保留，不重要统计可延后
- 侧边筛选在窄屏折叠成抽屉
- 卡片宽度减少后，信息密度要同步收敛，不保留桌面端所有元信息

---

## 6. 核心组件规范

### 6.1 TopNav

- 背景使用 `--bg-1`
- 底部分隔线可选 `1px solid var(--line-1)`
- Logo 强对比，导航项弱对比
- 激活态只允许使用白色文本、亮底胶囊或细下划线

### 6.2 HeroBanner

- 必须有黑色遮罩层，确保文字可读
- 文案结构固定为：标签 / 主标题 / 简述 / 主次 CTA
- 主标题允许大字号，但不做花哨字效
- CTA 一主一次，主按钮白底黑字，次按钮透明黑底配浅描边

### 6.3 FilterBar

- 筛选项使用胶囊结构
- 默认态：深灰底 + 浅描边 + 次文本
- 激活态：亮底或更亮边框，但仍保持黑白
- 搜索框与标签筛选保持同一圆角和高度体系

### 6.4 VideoCard

- 卡片由 `封面区 + 信息区` 组成
- 卡片外壳使用 `--bg-2`，不使用封面平均色作为底色
- 时长、来源、状态放在封面角落时，必须使用黑底半透明标记
- 信息区内标题最多 `2-3` 行
- 统计区、标签区、操作区保持固定位置，避免每张卡片结构飘移

### 6.5 DeferredPlayer

- 默认展示高质量封面和显式播放按钮
- 点击后才加载真实播放器
- 播放层按钮和提示文案使用黑白系统
- 播放器容器优先大圆角、大留白、深色背景

### 6.6 Tag / Chip

- 统一使用小号胶囊
- 默认深底浅边，选中可反转
- 单页内标签样式不得混杂多个视觉体系
- 标签数量过多时优先折叠，不挤压标题与主体内容

### 6.7 Button

- 主按钮：亮底黑字
- 次按钮：深底浅边白字
- 危险、成功、提示按钮仍旧使用黑白语义表达，不引入彩色
- hover 仅改变亮度、边框或轻微抬升

### 6.8 SearchField / Input

- 使用深灰背景或深底描边
- 输入框高度和筛选胶囊对齐
- placeholder 使用 `--text-3`
- focus 时只提升边框与背景层次，不出现彩色外圈

### 6.9 Sidebar / Drawer

- 背景使用 `--bg-1` 或 `--bg-2`
- 与主区之间通过边框和灰阶分层，不依赖彩色区分
- 收藏夹、分类、筛选入口应优先用纵向节奏组织
- 抽屉头部和主体区要有稳定的 padding 体系

### 6.10 Empty / Loading / Skeleton

- 空状态插图必须保持黑白或线性灰阶
- 加载骨架使用深浅灰块，不使用品牌色 shimmer
- 空状态文案克制，强调“等待内容”而不是营销文案

### 6.11 基础 UI 组件落地规则

前端实现必须优先复用 `src/components/ui/*` 下的基础组件，不允许页面为同一种控件反复手写完整 class：

- `Button` / `buttonVariants`：用于主按钮、次按钮、文本按钮、pill 按钮和链接式 CTA。
- `IconButton`：用于搜索、设置、通知、分享、关闭等纯图标操作。
- `Chip` / `chipVariants`：用于筛选项、标签、状态胶囊、计数胶囊和分页数字。
- `TextField`：用于单行输入框，统一 label、图标、输入高度、圆角、背景、边框和 focus 状态。
- `FormMessage`：用于错误、成功、信息、加载状态提示，必须配合 SVG 图标。
- `DialogShell`：用于弹窗、认证面板、投稿面板等居中对话框，统一遮罩、面板、标题区和关闭按钮。

页面组件可以通过 `className` 做少量布局适配，例如宽度、局部 gap 或特殊字号，但不得覆盖组件的核心颜色、圆角、边框和交互状态。如果确实需要新的视觉语义，应先扩展基础组件的 `variant` 或 `size`，再由页面复用。

### 6.12 封装触发规则

同一类 UI 满足任一条件时，必须抽到 `src/components/ui/*` 或对应业务组件目录，不继续在页面内复制：

- 同一段 Tailwind class 或同一控件骨架在 2 个以上文件出现。
- 同一交互角色出现 2 次以上，例如图标按钮、筛选胶囊、表单输入框、弹窗关闭按钮。
- 同一视觉语义出现 2 次以上，例如状态提示、标签、计数、分页数字、卡片角标。
- 组件需要统一 hover、focus、disabled、loading、active 状态。
- 组件会承载可访问性属性、图标、键盘状态或表单状态。

封装后页面只负责数据、布局和组合；控件的尺寸、间距、颜色、圆角、边框和状态表现由基础组件控制。

---

## 补充约束

### A. 可访问性
- 除了后端数据本身的文案外，写死的文案均为中文简体
- 正文与背景对比度必须满足可读性要求
- 小号灰字不能直接落在复杂封面上
- 关键操作必须有清晰 hover / focus / active 差异

### B. 设计与实现一致性

- Stitch 出图与真实前端都必须遵守同一套 token
- 组件先系统化，再页面化
- 页面可以变化，token 不随页面随意变化

### C. 最终判断标准

当去掉所有视频封面和作品图后，页面依然应当成立，并且仍然清楚表达层级、结构、交互和品牌感。这说明系统真正是黑白 UI，而不是依赖内容色彩撑场面。
